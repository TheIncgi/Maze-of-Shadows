package app.engine.entity.controllers;

import app.Game;
import app.engine.entity.Entity;
import app.ui.elements.SettingsPane;
import javafx.scene.input.KeyCode;
import javafx.scene.input.KeyEvent;

public class UserInput {
	private Entity entity;
	
	public UserInput(Entity e) {
		entity = e;
	}
	
	public void onKeyPress(KeyEvent keyEvent) {
		
		
//		if(key.equals( settings.getUpKeycode() )) {
//			dy = -1; //negative y is up
//		}
//		if(key.equals( settings.getDownKeycode() )) {
//			dy += 1;
//		}
//		if(key.equals( sett))
		
	}
}
